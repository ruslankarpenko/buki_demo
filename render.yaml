services:
  - type: web
    name: buki-telegram-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python buki_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false